This is my first step into blockchain.

• Setup:
It's a node implementation... soo... do the `npm` things 🙃

- To watch ts files run `npm run dev`, which is just `tsc -w`

• To run:

`npm run start` which is just `tsc && node .` in the command line
